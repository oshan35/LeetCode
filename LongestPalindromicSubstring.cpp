#include<iostream>
using namespace std;
#include<string>

string longestPalindrome(string s) {

    int longestPaldromicSubstring=0;

    for (int i = 0; i < s.length(); i++)
    {
        if ( i!=0 || i!=s.length()-1 )
        {
            
        }
        
    }
    
}

int main(){

    return 0;
}